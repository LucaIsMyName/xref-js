// src/__tests__/prefetch.test.ts
import { setupPrefetching } from '../prefetch';

describe('prefetch', () => {
  test('setupPrefetching function exists', () => {
    expect(typeof setupPrefetching).toBe('function');
  });
});